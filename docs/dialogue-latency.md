# 对话等待与推理模型兼容

## 2026-09-12：当前连续会话的正文输入预算

以 `48a7822` 为基线，本轮为 `PromptAssembler` 的原生 `CurrentConversation` 小节设置默认 **8,000 字符**预算。
按 `.NET String.Length` 统计整个小节，包含角色、换行、数据边界及转义、标题、介绍和省略提示；不是 token 上限。
短会话完整保持。长会话优先当前未答消息和最近最多四个完整 exchange，再有限补回本轮相关的旧轮次、
承诺/拒绝/取消/更正的原文证据及邻近后继。输出仍按原顺序排列，并标明缺口及有限摘录范围。

放不下的旧轮次整组跳过；异常超长的最新玩家消息以不可用及请缩短后重述的明确提示处理，不裁成半句或改答旧问题。
正常输入框已有 500 字符上限。过长的第三方 `CurrentConversation` 覆盖内容回退到原生预算视图。
选择在本地完成，不生成模型摘要，也不增加请求。启发式线索不能保证保留任意旧事实，当前权威状态和有效事项继续独立提供。

本次只限制正文提示词投影。完整会话缓存、持久化、最终提交和本地规则保留原文；动作同意和较早拒绝不由摘录裁决。
辅助远端请求沿用原有窗口，往日历史仍使用单独的 4,000 字符预算，整包提示词可以超过 8,000 字符。未改展示或动作提交时序。
隔离完整回归 **2,641 通过、0 失败、1 项原有跳过**，本轮新增 44 项用例；12 组中英离线测量全部通过。
中文普通台词的当前会话小节在 4 / 40 / 200 轮时分别由 688 / 8,740 / 44,780 字符变为 688 / 1,863 / 1,877 字符。
200 轮英文长台词从 219,830 变为 7,464 字符，仍保留样例中的相关旧事实、承诺后的取消和当前问题。
测量基线来自同一程序集的未限长视图，整包对照为算术重建；不是旧版独立运行或模型耗时实测。
原始请求哈希与固定前缀校验通过；本轮没有真实模型请求或游戏内验证，未部署、未推送。
详细测量口径与限制见[本地上下文召回说明](context-retrieval.md)，数据见[会话预算基准](benchmarks/current-conversation-20260912.json)。

## 2026-09-12：其他上下文的本轮召回与真实请求对照

本节记录已提交的 `48a7822` 批次，早于上节的当前连续会话预算。

长期记忆、偏好和已知社区印象增加 104 组受控中英等义词；共同经历、已结束求助、已解决冲突按本轮问题选择可选历史，
未完成事项、有效行为倾向、未解决冲突、未领金币奖励和应出现的一次性接续提示固定保留。
往日对话/事件可查询最近 20 条之外的已有记录，并在 4,000 字符内保留完整记录与近期接续。
同时修复只读关系快照消费提醒、过滤记录挤占历史配额、超长记录挡住全部历史，以及求助步骤状态被误认成顶层状态的问题。
查询选择在本地完成，不增加模型路由或 embedding 请求；该批次仍完整直接提供当前会话和角色身份。

以 `8bbc9b1` 为基线，真实生产装配器与相同世界检索结果的离线对照中，三个历史较多场景的整包输入减少 4.27%–6.68%；
无历史场景不变，活跃约束场景增加 313 字并补回旧版遗漏的 5 项有效约束。字符不是 token，也不是时延。
独立历史模块从 20 条 / 1,422 字符、漏掉目标，变为 3 条 / 308 字符、包含 40 天前目标；这一节省不叠加到整包统计。

使用配置的 `deepseek-flash` 运行三组成对合成请求，启用思考、Low 档、16,000 输出预算，均正常返回正文。
问候主请求耗时旧→新为 6.762→3.423 秒，旧经历为 6.133→5.305 秒，跨语偏好为 3.153→5.030 秒。
每个场景只有一对样本，缓存命中和推理/输出长度不同，不能声称稳定提速。生产严格内联元数据解析仅 4/6 通过，
新旧问候都缺少元数据，需要原有分类器回退；未计该追加请求，因此不代表完整游戏展示耗时。
旧经历在新版输入中找回，但真实回复仍补写了记录外细节，召回改善不等于事实一致性已经解决。
该批未实现向量检索或提前显示正文，当时当前会话尚无长度上限；连续会话的后续处理见上节。

完整回归在隔离副本通过 2,597 项、0 失败、1 项原有跳过；基准工具 9 项 Python 测试通过，构建关闭部署与打包。
实现范围、全部测量口径及限制见[本地上下文召回说明](context-retrieval.md)和[脱敏数据](benchmarks/context-retrieval-20260912.json)。

## 2026-09-12：按场景提供动作合同

Conversation / Gift 的隐藏元数据规则拆成两部分。关系变化、情绪、记忆、冲突、行为影响、结束对话、
字段类型与证据约束继续固定提供；赠礼、金钱、出行、节日、新求助、既有求助更新的详细字段按场景选取。
固定规则仍位于 NPC 的稳定前缀；变化的 `SceneActionContract` 放在动态尾部，不交给上下文路由删掉或第三方数据覆盖。

选择在本地完成，读取已捕获的当前玩家输入、实际赠礼机会/限制、求助 readiness、未完成请求、交付和正在进行的出游。
“好呀”等短续接会参考同一话题最近最多六条安全交流，混合协商合并所需类别；空白、被过滤的历史和明确的新话题形成边界。
当前事件状态在游戏主线程捕获，因此节日现场的短回答也保留节日字段。通用禁令、旧记忆中的动作词和模型字段名不构成机会。
未识别的能力上下文、矛盾/缺失的能力标记及不支持的语言保留完整动作合同。

这只是字段说明的选择，不授予动作权限。`complete:true` 仍表示本轮效果已完整分类；需要未提供的动作字段时，
模型必须返回 `complete:false`，由原有完整分类器恢复。原有明确递交礼物检查继续保留；新增的保守检查也会拦下
部分“未提供出行/新求助字段，但正文已经明确承诺且完全没有对应元数据”的情况。该检查不会创建动作或任务，
也不是任意句式的语义判定器；金钱、节日和既有请求更新不另加猜测性检测。

已有任务的纯提醒不产生新请求/更新，也不因重复说出物品而额外分类。多项求助的物品身份、必需步骤与顺序仍须保留，
不能附加可选物品；即时出游仍需要玩家邀约、明确同意及支持的目的地，邮件/以后赠礼仍不发放即时物品。
解析、全文分类回退、运行时权限和展示后单次提交流程继续执行。有效的完整 inline 回复仍只需一次正文请求。

以 `955a2f8` 的生产 DLL 为基线，用新旧两个独立进程装配同一份合成输入。合同统计如下；固定通用部分为 4,343 字符：

| 场景 | 旧完整合同 | 新通用 + 场景合同 | 合同字符变化 |
| --- | ---: | ---: | ---: |
| 问候 / 偏好 | 6,090 | 4,529 | -25.6% |
| 当前赠礼 | 6,090 | 4,896 | -19.6% |
| 新求助 | 6,090 | 5,387 | -11.5% |
| 既有任务提醒 | 6,090 | 4,735 | -22.2% |
| 出行邀约 / 短续行 | 6,090 | 5,009 | -17.8% |
| 多类动作同时相关 | 6,090 | 6,442 | +5.8% |
| 未知上下文，完整回退 | 6,090 | 6,748 | +10.8% |

以上是包含换行的 UTF-16 字符数，不是 token 或时延。多模块与未知情况保留较多字段，新增的完整性与复述规则会使其更长；
此次不保证所有场景都缩短。真实 default/zh 提示词、Penny 传记和原版世界全文参与装配，
固定 Town / 4 心 / 第 2 年春 15 日 13:30，机会与对话为合成值，没有游戏字符串预处理、内容包覆盖、画像匹配或存档历史。
18 个中英文场景中，合同以外部分的总字符数一致。中文问候整包由 22,679 到 21,118（-6.88%），
出行邀约由 22,681 到 21,600（-4.77%）；未知上下文则由 21,945 到 22,603（+3.00%）。
这里固定完整上下文路由与同一份世界全文，不重新评测世界检索，也不代表每次游戏请求的实际长度。
全部 18 个场景、64 种动作组合、程序集和输入哈希见[离线记录](benchmarks/scene-action-contract-20260912.json)，
可用[测量脚本](../tools/measure_scene_action_contract.ps1)复现；没有进行模型时延测量。

补充口径：这一旧脚本直接叠加 default/zh 字典，没有运行生产 `MergeLocalizedPrompts` 的继承性别变体清理，
所以中文整包绝对值仍是该脚本的合成值；上述合同本身及新旧合同差值不受影响。后续其他上下文基准已使用生产合并方法，不能将两批绝对值直接相减。

只含本批代码的隔离副本通过完整回归：2,499 项通过、0 失败、1 项原有跳过；此前针对性回归 318 项通过。
覆盖合同全集与运行时 DTO 的一致性、各场景字段、混合短续接、未知/过滤边界、节日快照、完整分类回退和正常请求次数。
构建关闭部署与打包，未调用线上模型或进行游戏内测速，游戏目录中的模组 DLL 未改变。

另完成两项评估，均未实现：

- [向量检索评估](vector-retrieval-assessment.md)：当前词项检索继续默认使用；先在长期记忆/偏好上验证跨语言和换说法召回，世界向量检索后置。
- [提前显示正文评估](early-dialogue-display-assessment.md)：先建立不重播、正文稳定、最终单次提交的展示会话，再考虑完整正文边界 `BodyReady`；缓冲 SSE 本身不等于提前显示。

## 2026-09-12：本轮输入驱动的本地召回与世界条目检索

输入框和对话选项现在把本轮玩家文本显式传到记忆召回与世界检索。长期记忆、玩家偏好和已知社区印象
先按当前问题的相关性排序，再参考原有场景、重要度和时效；空输入或完全无匹配时保留原排序。
右键开场、定时对白和送礼不把旧聊天问题当成本轮查询。查询只在本次请求中使用，不新增存档字段，
回忆计数仍由原来的标记流程更新。联机客户端继续使用主机发布的关系摘要，此次未增加逐轮记忆同步协议。

世界资料按人物、地点、季节和节日建立本地索引，支持中英文实体别名、常见话题词和内容包自定义 `Aliases`。
中文记忆使用相邻双字匹配，世界检索另有中英别名表；这是词项检索，不是向量语义检索，也不会额外请求模型或 embedding。
已有语义路由开关继续生效，世界模块及其事件历史依赖会在检索前正确展开。

固定世界背景留在可缓存的前缀，按本轮选中的完整条目放入末尾的 `world_retrieval` 数据块。
当前人物、季节、地点、目的地和当天节日作为场景线索；没有独立地点条目的区域采用有界补充。
普通检索默认约 8 条、6,000 字符，明确点名和必要场景条目可超过软上限，不截断单条事实。
要求列举整类或介绍整个世界时会扩大对应范围；普通未命中只保留固定背景和场景资料，不能据此断言 NPC 不知道其他事。
“那里怎么走”等明确续问可参考紧邻前文，新问题的明确主题优先于旧话题。

索引及本地化文本在游戏主线程捕获，后台只查询不可变数据。内容或提示词失效后新请求重建索引，
已在途请求继续使用原快照；全量/精简、原版/SVE 四个变体分别缓存。异常索引或输入可用已捕获的完整资料回退，
运行时捕获失败不会从工作线程重新读取游戏资源。原有第三方内容过滤、数据边界和动作授权约束继续生效。

使用本次隔离构建、仓库中的真实世界资料和中文渲染键，离线测量了 4 个变体 × 11 个合成问题。
固定场景为 Penny / Town / spring；“图书馆在哪里？”的世界文本如下：

| 变体 | 原全文字符 | 固定核心 + 检索条目 | 减少 |
| --- | ---: | ---: | ---: |
| 原版完整 | 11,714 | 2,269 | 80.6% |
| 原版精简 | 7,786 | 1,682 | 78.4% |
| SVE 完整 | 16,014 | 2,269 | 85.8% |
| SVE 精简 | 10,332 | 1,682 | 83.7% |

以上只统计世界文本的 UTF-16 字符数，不含外层数据标记或其他提示词模块，不能视为整包输入、token 或耗时降幅。
“列出所有世界资料”在四个变体分别保留 67、61、95、89 个条目，全部比原全文多 144 字符，没有压缩收益。
每个场景暖机 20 次、测量 100 次，离线 `Retrieve` 中位数为 0.056–0.490 ms，最高 P95 为 0.647 ms，
单次最大值 6.302 ms。计时环境是 PowerShell 7.6.5 / .NET 10.0.11，加载的是目标 net6.0 的 Debug 程序集；
不含索引构建、游戏资源读取、网络或模型推理，不代表实际游戏速度。完整口径、资源哈希及全部场景见
[离线记录](benchmarks/world-retrieval-20260912.json)。

只含本次改动的隔离副本通过完整回归：2,312 项通过、0 失败、1 项原有跳过。
验证未调用线上模型，构建显式关闭部署与打包，游戏目录中的模组 DLL 未改变。

## 2026-09-12：状态表达与行为上下文去重

- 正式对话输出预算提高到 16,000；推理与可见对白共享该预算，上限不会强制模型生成到这么长。
- `deepseek-flash` 与 DeepSeek v4 的 `Low` / `Minimal` 均发送 `reasoning_effort: low`。
  带供应商前缀、版本或渠道后缀的模型名按完整家族名匹配，旧 R1 / reasoner 映射保留。
- 历史模板统一日期、正文、说话者、旁观者、事件与听众参数。旧参数保留可明确解释的别名；
  未展开、丢失正文或混淆旁观者角色的模板回退到完整事实行。节日历史保留对白，缺失活动事件键回退到实际存在的文案。
- 约会、订婚、婚姻、室友和离婚按当前明确状态互斥表达，心数不再额外叠加矛盾的求偶指导。
  旧约会模板仍可取得中性的关系称谓。低模组熟悉度表示记录有限，不能据此否定当前友谊或婚姻；
  互动节奏的心数标明来自上次记录，场景建议与已应用状态分别表达。
- 人格风格、当前状态及回复指导各自只保留一份。优先连续性已带出的礼物、事件、冲突、求助和共同经历，
  不再重复写入持久记忆摘要；其他记录、具体步骤与物品 ID、知情和隐私边界继续保留。

以 Git `5b11382` 的源码构建为基线，同一份 `BuildRichPromptFixture()` 合成行为上下文从
8,422 缩短到 6,423 字符（减少 1,999，约 23.7%），11 项选定事实在新版全文中全部各出现一次。
这是 C# 字符长度，不是整包输入、token 或延迟降幅；旧 Brief 模式仍会省略部分记忆，不能据此称其无损。

只含本次改动的隔离副本通过完整回归：2,209 项通过、0 失败、1 项原有跳过。
验证使用本地模拟请求、真实提示词资源及合成状态，不调用线上模型；构建显式关闭部署与打包。
此批未修改检索、语义路由算法和元数据协议；后续的本地条目检索见上节。

## 2026-09-10：缩短输入与空响应诊断

本轮使用 `OpenAiCompatible / gpt-5.6-sol`，正文保持 `Low`，语义路由保持关闭。
完整密集合成场景的线上测量输入由 45,498 缩短至 37,202 字符，减少 8,296 字符（18.2%）；
**这确认了输入缩短，尚不能确认实际对话稳定提速**。所有请求均为合成内容，没有读取或修改玩家存档。
逐次参数、顺序、耗时、输入哈希、已报告用量和离线导出证据见
[本轮脱敏测量记录](benchmarks/dialogue-latency-20260910.json)。

密集场景通过实际生产 `BehaviorPromptContextBuilder` 与 `PromptAssembler` 构建，使用
完整上下文计划且 `UseOptimizedPrompts=false`。下表是实际发送输入的快照，不代表后续修订后的最终构建：

| 提示词分段 | 旧版字符 | 压缩版字符 |
| --- | ---: | ---: |
| 世界常量 `GameConstantContext` | 14,658 | 11,902 |
| 行为上下文 `LivingNpcExtraPrompt` | 17,727 | 14,753 |
| 指令及外围格式 `Instructions` | 10,032 | 7,413 |
| 末尾命令 `Command` | 80 | 133 |
| 其他分段合计 | 3,001 | 3,001 |
| 完整输入 | 45,498 | 37,202 |

字符统计不等于模型 token 数、账单降幅或延迟降幅。独立 SVE 离线导出的拍照样例中，世界常量
从 20,055 变为 16,161 字符，完整输入从 33,667 变为 27,207 字符；该样例使用较短的合成行为上下文，
不属于密集场景，也没有为此另发线上请求。测量记录保留各快照的程序集、资源和提示词哈希。

事实复核后补回了 Clint 经营铁匠铺的明确表述，并保留 SVE 两处描述原有的方向和认知语气。
最终构建再次离线导出：密集输入为 **37,219 字符，比旧版减少 18.2%**；SVE 拍照样例为 27,236 字符。
两份世界资料的 105 个文本字段合计从 17,703 缩短到 13,746 字符（22.4%），
96 个实体及其他字段、列表和顺序均保留。最终这些导出没有再次发往模型。

本轮实现的输入调整：

- 世界资源合并重复叙述，保留角色、地点与场景事实，继续使用完整世界上下文。
- 内联元数据指令本体从 8,656 缩短到 6,090 字符（29.6%）。完整 JSON schema、
  `complete` 稀疏协议及解析规则保留，赠礼证据与低分偏好的分类器回退不变。
  此处统计的是独立方法输出，与表中含外围格式的 `Instructions` 口径不同。
- 行为上下文删除已经逐字包含在 stance 中的重复资料。只有在没有 `Offered` / `Pending`
  求助且 readiness 明确禁止新请求时，才省去候选和生命周期说明；禁止规则及原因仍提供。
  已有求助或允许新请求的场景继续保留机会、物品、步骤、接受、阶段和路线约束。
- 固定的世界、角色及指令内容排列在可变会话内容之前，便于相同前缀复用。
  这不保证兼容网关支持或命中缓存；本轮压缩版成功响应未报告 `cached_tokens`，不能据此验证新布局的缓存收益。

10 次顺序线上请求中，3 次返回非空正文、6 次空返回、1 次返回明确的供应商过载错误。
全部使用流式传输，HTTP 状态均为 200，返回的模型名均为 `gpt-5.6-sol`：

| 请求 | 输入字符 | 首段正文 / 完整响应 | 结果 |
| --- | ---: | --- | --- |
| 可用性筛选：旧版完整拍照 | 28,231 | 5.449 / 8.240 秒 | 非空正文 |
| 可用性筛选：手写短输入 | 517 | 11.336 / 13.824 秒 | 非空正文 |
| 路由筛选：Low | 2,682 | 无 / 3.741 秒 | 空返回 |
| 路由筛选：Off，JSON mode | 2,682 | 无 / 68.612 秒 | 空返回 |
| 密集 ABBA：旧版 A | 45,498 | 无 / 3.392 秒 | 空返回 |
| 密集 ABBA：压缩版 B | 37,202 | 无 / 3.820 秒 | 空返回 |
| 密集 ABBA：压缩版 B | 37,202 | 无 / 3.680 秒 | 空返回 |
| 密集 ABBA：旧版 A | 45,498 | 无 / 3.992 秒 | 空返回 |
| 同一压缩版输入的诊断补测 | 37,202 | 22.677 / 30.275 秒 | 非空正文、真实 `[DONE]` |
| 后续偏好功能请求 | 27,092 | 无 / 8.968 秒 | 明确过载，停止后续付费请求 |

ABBA 四次全部失败，约 3–4 秒结束的是空响应，不能当作生成速度。后来的单次压缩版成功也没有
同时期成功的旧版对照。两条可用性筛选的内容不同，且使用较短的合成行为上下文，不能当作压缩前后实验。
路由 Off 样例同时改变了推理档位和 JSON mode；它不是仅改变推理强度的因果对照。
两条路由探针使用 SSE，生产快速路由使用非流式调用，所以它们也不是生产路由超时的完整复现。
正文请求的输出预算均为 2,048，路由为 384；此模型通过 `max_completion_tokens` 发送预算。

诊断补测在 10.904 秒收到响应头、22.677 秒收到第一段可见正文，30.275 秒收齐；记录了
86 个 SSE 事件与真实 `[DONE]`，未收到错误信封。正文携带 `!LIVINGNPCS_META {"complete":true}`，
经过实际生产解析与清理代码检查，正文可解析、严格内联元数据有效、元数据不泄漏到可见文本，
无需分类器回退。这只验证了这一条普通拍照对话。

后续偏好请求在 HTTP 200 的 SSE 流中明确返回 `provider-error`：
`Our servers are currently overloaded. Please try again later.`，没有正文或用量。
该证据只能解释这一条请求；此前六次空返回没有捕获到同样的错误信封，不能回溯认定为同一原因，
也不能从客户端时间拆出排队、上游计算或网关内部处理分别用了多久。

三条成功响应合计报告 30,585 个输入 token、612 个输出 token，其中报告了 359 个推理 token。
只有前两条筛选请求报告了缓存输入，共 8,064 个 token；这些缓存数据来自旧输入，不能证明新前缀排列产生了命中。
未报告的用量与缓存命中量仍为未知，不按零处理，这些合计也不是完整账单。

传输侧把仅有 `data: [DONE]`、没有正文的流视为已完成但失败的终态，避免跨候选或通道再次生成。
本轮还修复了实测暴露的错误信封遗漏：HTTP 200 内非空的顶层 `error` 现在按服务失败处理，
不再把它当空正文而切换 SSE / JSON / instructions 请求格式。只保留原格式的有限重试；
正文调用的 `AllowRetry=false` 在客户端只发一次，引擎仍保留原来的最多两次尝试。
真正的 HTTP 400/422 参数拒绝仍可兼容回退。标准 JSON、SSE、忽略 stream 后返回的缩进 JSON
都受检查，正文中途出现错误时丢弃结果。生产日志只记录固定的错误类别，不回显供应商原始错误内容。

新增的生产诊断在正文生成成功或最终失败时，按该阶段的每次 HTTP 请求记录
`headers`、`first-content`、`complete`，并附最终响应的输入、缓存、输出、推理 token 及用量来源。
不能观察的时间点或未报告的缓存、推理 token 显示 `n/a`，显式报告的零仍显示 `0`。
非流式调用只能可靠提供完整耗时。复测脚本补充 SSE 事件数、真实结束标记、顶层字段名与错误信封摘要，
对错误消息、被回显的字段名和其他响应字符串统一脱敏；异常 JSON 结构可记录为失败，
HTTP 200 错误信封会立即停止后续探针。提交的记录通过字段白名单排除了正文、原始提示词、端点、凭据及本机绝对路径。

偏好、赠礼、求助等 `sol` 线上功能覆盖因服务异常未完成，相关代码路径由本地回归覆盖。
下方 2026-09-09 的八类线上输出与本地处理验证属于 `gpt-5.6-luna` 历史记录，不能计作本轮 `sol` 验证通过。

最终验证：.NET 全量测试 **2,102 通过、0 失败、1 项原有跳过**；7 项 Python 离线探针测试通过；
离线诊断 8/8 通过；Release 构建成功，81 项既有警告、0 错误。错误信封新增的 21 项回归在修复前
有 15 项失败，修复后全部通过。唯一成功的密集拍照回复也已用最终 Release 的生产解析器再次校验通过。
构建和验证均禁用自动部署；本机配置继续保留 `gpt-5.6-sol / Low`、关闭语义路由、开启后台流式传输，
不启用旧的 optimized / concise 删减开关。

## 2026-09-09：单次生成与真实请求验证

在用户授权后，使用游戏配置中的 `OpenAiCompatible / gpt-5.6-luna` 发送了
29 次顺序测试请求，全部使用合成对话。18 次返回非空正文，11 次出现空正文、
HTTP 503 或超时。请求参数、耗时与供应商返回的用量保存在
[脱敏测量记录](benchmarks/dialogue-latency-20260909.json)；记录不含端点、密钥或玩家存档。
有用量的响应合计报告 168,838 个输入 token、3,802 个输出 token（其中报告了
1,501 个推理 token）。失败请求缺失用量，因此这些数字不能当作完整账单。

本轮实施：

- 主动对话和送礼反应同时生成正文与末尾 `!LIVINGNPCS_META {"complete":true,...}`。
  严格检查完整 JSON、字段类型、重复字段、末尾位置与当前话语证据；通过时省去独立分类请求。
  缺失、损坏、明确递送礼物却漏写动作、偏好分值低于持久存储门槛时，仍调用原分类器。
  元数据协议与分类器共用，取消、正文清理、原生窗口及展示成功后提交的顺序保持一致。
- 明确记忆重要度采用 0–100，保存门槛为 40。最初实测中的偏好错误地给出 3/2 分，
  会被存储层丢弃；修订协议后的独立真实样本给出 70/65，经过实际偏好存储代码验证两条均保存。
  该样本缩短了场景上下文，只用于协议质量检查，不用它声称游戏提速。
- 新增可选 `UseStreamingDialogueTransport`。OpenAI 兼容端点在后台流式收齐主回复，
  然后交给原生对话框；不存在预览后重放。正文与用量、终止标记、断连、取消和整个请求的
  截止时间都受检查。快速分类请求仍走原通道。不支持流式的端点保留非流式兼容回退。
  开关默认关闭，便于其他兼容接口保持原配置；本机经过流式实测后启用。
- 502/503/504 不再通过切换消息格式或非流式通道反复请求不可用的网关。
  暂时故障只保留原格式的有限重试，400/422 的兼容回退仍保留。
- 世界阶段说明在有持久状态时只输出一份，样例减少约 1,031 字符。
  无持久状态时保留原有提示，避免末尾指导提前返回导致事实丢失。

本机部署配置关闭 `EnableSemanticContextRouting`，每轮直接使用完整上下文，省去前置模型请求；
保留 `ChatThinkingLevel=Low`、`RoutingThinkingLevel=Low` 以及原模型、端点和超时。
两个旧精简开关仍关闭：它们会删减部分世界地点、社区印象或复杂关系指导，不能把这种删减当成无损提速。
关闭路由也会使原先纯问候的简略上下文变为完整上下文，不能保证这类回合更快。

实测结果需要同时看成功、变慢和失败的样本：

| 对照 | 观察结果 |
| --- | --- |
| 同一短对话，Low / none，ABBA 顺序 | Low 6.95 / 10.54 秒；none 73.14 / 10.94 秒。其中一次 none 仍报告推理 token。 |
| 相同完整拍照场景 | 旧路由 12.22 秒超时 + 正文 37.91 秒 + 分类 12.01 秒超时，合计约 62.1 秒；合并流式请求 18.68 秒。 |
| 相同求助场景 | 旧正文与实际回复的分类合计 13.05 秒；合并流式请求 35.39 秒。两次生成的文本与输出长度不同。 |
| 首组完整上下文单请求 | 六条成功为 6.16–30.76 秒，两条在约 31/34 秒返回 HTTP 200 空正文。 |
| 后续完整上下文 | Low 与 none 都发生约 85 秒超时；一条有效求助耗时 73.74 秒。 |

因此可以确认减少了正常交互的串行请求数，但**没有证据支持稳定的全局提速倍数或秒回承诺**。
这条转发服务的排队、生成速度或可用性波动仍会决定等待上限，客户端无法从响应推断具体是哪一个。
新日志的路由、主请求、元数据、动作恢复计时可继续区分这些阶段。

8 类合成场景经过真实模型输出与本地处理链检查：普通问答、偏好、即时赠礼、延期邮件、
求助、接受邀约、明确边界、拒绝邀约。检查覆盖解析、物品 ID、偏好存储门槛、求助一致性及出行过滤，
不等于在运行中的游戏里实际发放物品或执行出行。完整上下文的偏好修订请求受到服务超时影响，
偏好修订的在线成功证据来自上述独立协议样本；低分原始输出另有分类器回退回归测试。

最终完整回归：2,044 项通过、0 失败、1 项原有跳过；离线诊断 8/8 通过。
新增测试覆盖单请求/回退次数、低分偏好真实存储、赠礼方向、SSE 用量、真实结束标记、
非流式兼容回退、503、空正文、流中断、取消、完整读取超时及展示前不提交副作用。

复测工具：[benchmark_dialogue_latency.py](../tools/benchmark_dialogue_latency.py)。
它只读取指定配置里的模型和凭据，默认执行四条合成短对话；`--fixtures` 可传入合成请求数组。
每个元素包含 `name/system/user/effort/stream/max_tokens`，可选 `json_mode`、`timeout_seconds`
及 `metadata_followup`（用实际主回复填入导出的分类器数据块）。工具不自动重试，遇到鉴权、
限流或网关不可用错误停止，记录不含隐藏推理。运行依赖 Python 和 `requests`。

## 2026-09-08 版本记录

2026-09-08 的游戏日志记录了一轮 `gpt-5.6-luna` 对话总耗时 65,877ms，
其中语义路由在 12,000ms 超时并回退到 39,312 字符的完整提示词，动作补救也失败。
同批平台截图有 29.44 秒、68 秒、78 秒的单次请求。平台记录不能直接等同于
游戏中每个阶段的等待：客户端取消后，转发服务仍可能继续处理上游请求。

此前一轮交互依次等待路由、正文、完整元数据；元数据失败时还会等待动作补救。
元数据和补救的内部超时各为 12 秒。`12 + 29.44 + 12 + 12 ≈ 65.44` 秒与截图吻合，
但旧日志没有每段计时，不能据此认定某个平台记录必然对应其中某段。

## 本次修复

- DeepSeek v4 的 `Low` / `Minimal` 发送 `reasoning_effort: low`。此前误发 `high`。
  旧 R1 / reasoner 的能力映射不跟随 v4 修改。
- 空正文、`finish_reason: length` 或仅推理的终态响应不再跨候选、跨引擎反复重试。
  错误仅记录停止原因与 token 用量，不把 `reasoning_content` 当正文或打印整段内容。
- HTTP 400/422 直接切换兼容请求形式；瞬态错误继续保留重试。
  仅瞬态失败不删除思考控制，DeepSeek 的明确 Off 也不会因裸请求回退而被重新开启。
  鉴权失败立即结束；限流和连接故障只重试原请求形式，不重复切换消息格式或流式通道。
- 如果转发服务无视 `stream` 而返回完整 JSON，第一次收到就消费结果与真实用量。
- 只有整句为首轮问候时直接构建保守简要上下文，所有原有硬依赖仍会应用。
  实质问题和后续话轮继续语义路由；失败的完整上下文仅在当前会话复用，避免反复等待超时。
  NPC、会话、日期、连接、模型或端点变化会让缓存失效。
- 动作补救只由本轮实际话语、最近的约定或活跃求助触发。
  通用动作规则、字段名和“没有授权送礼”等背景文字不再让普通寒暄额外请求模型。
- 元数据支持 `{"complete":true,...}` 的稀疏完整回复，仅输出有变化的字段。
  省略项由程序填默认值；完整性标记、字段类型与已有证据约束仍会检查。
  老版完整结构继续兼容，没有完整性标记的残缺结构继续失败回退。
- 调试日志分别输出路由、正文、元数据和动作补救的结果及毫秒数。

正文、情绪、关系、记忆和世界动作仍在同一次生成中完成校验，展示成功后在主线程提交。
没有把副作用移到任意后台任务，以免玩家离开、取消或切换对话后提交过期结果。

## 验证与实际速度

回归测试使用可计数的本地客户端及模拟 HTTP，覆盖请求次数、模型参数、完整/稀疏元数据、
直接及多轮承诺、取消与展示后提交。普通首轮问候只需正文和元数据两次逻辑请求；
元数据失败也不会仅因通用规则而再启动动作补救。供应商内部仍可能因真正瞬态故障重试。
本轮完整测试为 1,899 项通过、0 项失败、1 项原有跳过。

没有使用用户密钥做付费在线压测，因此本次不报告一个未经实测的提速百分比或秒数。
`Low` 降低推理强度，不会消除推理、网络传输及供应商排队。
新日志可分清时间花在何处；单次正文请求若仍长达几十秒，客户端改动无法保证其秒回。
现有模型选择、思考档位和超时配置保持用户设定。

官方参数依据：

- [DeepSeek Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode)：v4 支持 low/high/max，默认开启思考。
- [OpenAI Reasoning](https://developers.openai.com/api/docs/guides/reasoning)：推理与可见回复共享输出预算，推理强度影响耗时。
